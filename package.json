{
  "name": "super-huffman-64",
  "version": "0.0.1",
  "description": "Encodes/decodes a string using Huffman compression to a pseudo-Base64 string",
  "main": "index.js",
  "scripts": {
    "clean": "tsc --build --clean",
    "test": "npx jest huffman.test.ts",
    "build": "npx tsc --build",
    "watch": "npx tsc --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tinyqueue": "^2.0.3",
    "tslib": "^2.1.0"
  },
  "devDependencies": {
    "@babel/core": "^7.13.1",
    "@babel/preset-typescript": "^7.13.0",
    "@types/jest": "^26.0.24",
    "jest": "^26.5.2",
    "ts-jest": "^26.5.2",
    "typescript": "^4.4.4"
  }
}
